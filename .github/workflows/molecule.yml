on: push

jobs:
  molecule:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - uses: actions/setup-python@v4
        with:
          cache: pip

      - run: python3 -m pip install ansible molecule yamllint molecule-docker

      - run: molecule test
